const axios=require('axios'); const backend=process.env.BACKEND_URL||'http://localhost:4000'; const products=['ARK-DP','HLM-CASH','HLM-TRANS']; async function run(){ for(const p of products){ try{ const r=await axios.post(backend+'/admin/run-compensation',{product_id:p}); console.log('ran',p,r.data); }catch(e){console.error('err',p,e.message);} } } setInterval(run,60*1000); run();